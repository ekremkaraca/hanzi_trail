<%= render Reviews::PageComponent.new(
  flashcard: @flashcard,
  remaining_due_count: @remaining_due_count,
  empty_state_title: @empty_state_title,
  reviewed_count: @reviewed_count,
  filter_params: @review_filter_params,
  total_review_count: @total_review_count,
  reviewed_today_count: @reviewed_today_count,
  show_pinyin: session[:show_pinyin]
) %>