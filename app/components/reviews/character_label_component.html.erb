<div class="<%= wrapper_classes %>">
  <span class="is-size-7 <%= color_class %> text-uppercase tracking-wide"><%= label %></span>
  <div class="is-size-6 mt-1"><%= simple_format(flashcard.public_send(attribute)) %></div>
</div>
